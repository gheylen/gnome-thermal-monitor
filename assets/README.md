# Assets

Project images used by the README and the extensions.gnome.org listing.

## Wanted

- **`screenshot.png`** — the panel indicator in the top bar with the popup menu
  open, ideally showing at least one non-nominal state (orange or red). Once
  added, uncomment the image reference near the top of the root `README.md`.

Keep images reasonably sized (PNG, < ~300 KB) so the repo stays lean.
